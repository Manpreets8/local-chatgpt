# Dependencies are added incrementally as each feature is built,
# not all at once. This file currently covers: scaffolding + chat + memory.

# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1

# --- AI / LLM ---
anthropic==0.40.0

# --- Database ---
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
alembic==1.14.0

# --- Testing ---
pytest==8.3.4
httpx==0.28.1
