# Dependencies are added incrementally as each feature is built,
# not all at once. This file currently covers: scaffolding + chat +
# memory + document upload + auth + image generation.

# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
python-multipart==0.0.20
email-validator==2.2.0

# --- AI / LLM ---
anthropic==0.40.0

# --- Image generation (Pollinations.ai — free, keyless HTTP API) ---
httpx==0.28.1

# --- Database ---
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
alembic==1.14.0

# --- Auth ---
bcrypt==4.2.1
python-jose[cryptography]==3.3.0

# --- Document processing ---
pypdf==5.1.0
python-docx==1.1.2

# --- Testing ---
pytest==8.3.4
